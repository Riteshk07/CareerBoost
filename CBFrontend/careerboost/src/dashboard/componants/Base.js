import React from 'react';

const Base = () => {
    return (
        <div className="dashboard">
            <h2>Welcome to the Dashboard</h2>
            {/* Add other sections here */}
        </div>
    );
};

export default Base;
